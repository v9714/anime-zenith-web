
// Import from the actual implementation
import { toast } from "@/hooks/use-toast";
import { useToast } from "@/hooks/use-toast";

// Export the hooks for usage throughout the app
export { useToast, toast };
